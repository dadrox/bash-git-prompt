#  ► ▶ ᐉ    
# ⟦ ⟧ ⟨ ⟩ ⟪ ⟫ ⦃ ⦄ ❰ ❱ 
# ⟰ ⟱ ⬆ ⬇ ⋀ ⋁
# ◯ ◌ ◍ ◎ ● ◯ ∅
# ✕ ✖ ✗ ✘
# ★ ☆ ☾ ☽ ☉ ⴲ
# ▲ △ Δ   ⃤ 𝚫 𝛥 𝜟 𝝙 𝞓 ∇
# … ⌁ ⋮	⋯ ∙ ∷
# ϕ ↳ ⌂
# (╯°□°）╯︵ ┻━┻
# ッ⍣ ⍤
# ✔

GIT_PROMPT_LEADING_SPACE=0

  # These are the color definitions used by gitprompt.sh
  GIT_PROMPT_PREFIX="⟨"                 # start of the git info string
  GIT_PROMPT_SUFFIX="⟩"                 # the end of the git info string
  GIT_PROMPT_SEPARATOR="$Normal:"     # separates each item

  GIT_PROMPT_BRANCH="$Green"          # the git branch that is active in the current directory
  GIT_PROMPT_STAGED="$Green✚"         # the number of staged files/directories
  GIT_PROMPT_CONFLICTS="$Bold$Red✖"   # the number of files in conflict
  GIT_PROMPT_CHANGED="$Yellow▲"       # the number of changed files
  GIT_PROMPT_REMOTE=" "                 # the remote branch name (if any)
  GIT_PROMPT_UNTRACKED="$Red△"        # the number of untracked files/dirs
  GIT_PROMPT_STASHED="$Blue◎"     # the number of stashed files/dir
  GIT_PROMPT_CLEAN="$Bold$Green✔"      # a colored flag indicating a "clean" repo
